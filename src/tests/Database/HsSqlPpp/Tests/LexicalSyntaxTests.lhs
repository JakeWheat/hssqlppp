This is a stub for the combined tests/reference for what syntax is
supported. To mainly follow the contents of the postgresql manual in
the same order. TODO for 0.4.0


identifiers

quoted identifier

case preservation - not supported

unicode identifier - not supported

string constants

string constant with newline - not supported

string constant with c style escapes

string contants with unicode escapes - not supported

dollar quoted strings

bit string constants - not supported

numeric constants

casts

operators - add simple parsing support for every operator in the default template1 catalog
          - add simple support for other operators?

position paramas, parens, brackets, comma, semi coloin, colon, asterisk - see elsewhere

comments

lexical precedence - link to parse table, notes on missing bits

operator() syntax

